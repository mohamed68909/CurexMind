# 🏥 نظرة عامة على مشروع نظام إدارة العيادات (CurexMind)

هذا الملف يقدم شرحاً شاملاً وتفصيلياً لمشروع **CurexMind**، وهو نظام متكامل لإدارة العيادات الطبية يتكون من واجهة خلفية (Backend API) وواجهة أمامية (Frontend)، بالإضافة إلى مشاريع أخرى متعلقة بالاختبارات.

---

## 🏗️ هيكلية المشروع
المشروع مقسم إلى عدة أجزاء رئيسية:
1. **CurexMind.API**: الواجهة الخلفية (Backend) المبنية باستخدام .NET 9.
2. **CurexMind.Frontend**: الواجهة الأمامية (Frontend) المبنية باستخدام React و Vite.
3. **CurexMind.Tests**: مشروع خاص بالاختبارات (Testing).

---

## ⚙️ أولاً: الواجهة الخلفية (Backend - CurexMind.API)

الواجهة الخلفية عبارة عن **RESTful Web API** قوية وجاهزة للإنتاج، تم بناؤها باستخدام **ASP.NET Core (.NET 9)** و **Entity Framework Core**.

### 🌟 الميزات الأساسية:
- **إدارة العيادة (Clinic Management):**
  - إدارة الأطباء، المرضى، والإقامات في المستشفى.
  - إدارة كاملة للمواعيد (إنشاء، قراءة، تحديث، حذف - CRUD).
  - نظام تقييم ومراجعة للأطباء.
  - دعم التصفح عبر الصفحات (Pagination) والفلترة (Filtering).
- **حجز المواعيد عبر الإنترنت (للمرضى):**
  - يمكن للمرضى حجز مواعيدهم عبر الإنترنت.
  - نقاط نهاية (Endpoints) محمية بواسطة JWT.
  - التحقق من الصلاحيات بناءً على الملكية (لا يمكن للمريض الوصول إلا لمواعيده الخاصة).
- **النظام المالي (Financial System):**
  - إنشاء وإدارة الفواتير.
  - معالجة الدفعيات بشكل آمن.
  - إمكانية تصدير الفواتير كملفات PDF.
  - حماية الفواتير بناءً على ملكية المستخدم لها.
- **المصادقة والتفويض (Authentication & Authorization):**
  - مصادقة باستخدام رموز JWT.
  - تكامل مع ASP.NET Identity.
  - تفويض مبني على الأدوار (مسؤول Admin / مريض Patient).
  - التحقق من الملكية على مستوى الموارد (Resource-Level).
  - تكامل آمن مع Swagger مع دعم JWT.

### 🏗️ البنية التصميمية (Architecture):
يعتمد المشروع على بنية الطبقات النظيفة (Clean Layered Architecture):
`Controllers → Services → Data → Entities`

**أهم أنماط التصميم المستخدمة:**
- نمط طبقة الخدمات (Service Layer Pattern).
- كائنات نقل البيانات (DTOs).
- استخدام غلاف `Result<T>` لتوحيد شكل استجابات الـ API.
- حقن التبعية (Dependency Injection).

### 🧰 التقنيات المستخدمة في الباك اند:
- **اللغة والإطار:** C# (.NET 9), ASP.NET Core Web API.
- **قواعد البيانات (ORM):** Entity Framework Core, SQL Server.
- **التوثيق:** Swagger.
- **تحويل الكائنات (Mapping):** Mapster.

---

## 🎨 ثانياً: الواجهة الأمامية (Frontend - CurexMind.Frontend)

تم بناء الواجهة الأمامية لتكون سريعة وعصرية باستخدام أحدث تقنيات الويب.

### 🧰 التقنيات والمكتبات المستخدمة:
- **الإطار الأساسي:** React 19 مع TypeScript.
- **مُجمّع الحزم (Bundler):** Vite (مما يوفر سرعة عالية في التطوير والبناء).
- **إدارة الحالة (State Management):** Zustand (مكتبة خفيفة وسريعة لإدارة الحالة العامة للتطبيق).
- **التوجيه (Routing):** React Router v7 لإدارة التنقل بين الصفحات.
- **التنسيق (Styling):** Tailwind CSS v4، وهو أحدث إصدار لإعطاء تصميم عصري ومتجاوب.
- **الاتصال بالواجهة الخلفية (HTTP Requests):** Axios للتعامل مع الـ API.
- **التعامل مع الـ JWT:** مكتبة `jwt-decode` لفك تشفير رموز المصادقة.
- **الأيقونات والتنبيهات:**
  - `lucide-react` لعرض الأيقونات العصرية.
  - `sweetalert2` لعرض تنبيهات (Alerts) تفاعلية وجميلة للمستخدم.

---

## 🔒 سير عمل المصادقة والحجز

### سير عمل المصادقة:
1. يقوم المستخدم بالتسجيل أو تسجيل الدخول عبر مسارات `/api/Auth/sign-up` أو `/api/Auth/sign-in`.
2. يستلم العميل (Frontend) رمز **JWT**.
3. يتم إرسال هذا الرمز في ترويسة الطلبات (Authorization: Bearer) للوصول إلى المسارات المحمية.

### سير عمل الحجز للمريض:
1. يسجل المريض الدخول ويحصل على الرمز (JWT).
2. يطلب مسار الحجز: `/api/appointments/patient/{patientId}/book`.
3. يتحقق النظام (Backend) من أن المستخدم مصادق عليه، ودوره "مريض"، وأن المعرف `patientId` يخص المستخدم المسجل حالياً، ثم يتم تأكيد الحجز.

---

## 🚀 كيفية تشغيل المشروع

**متطلبات التشغيل:**
- .NET 9 SDK
- SQL Server
- Node.js (للواجهة الأمامية)

**لتشغيل الباك اند:**
```bash
cd CurexMind.API
dotnet ef database update
dotnet run
```
ثم يمكنك تصفح الـ Swagger عبر `https://localhost:{port}/swagger`.

**لتشغيل الواجهة الأمامية:**
```bash
cd CurexMind.Frontend
npm install
npm run dev
```

---

## 🔮 التطويرات المستقبلية المقترحة
- إضافة المزيد من الاختبارات (Unit/Integration Tests) باستخدام xUnit و Moq.
- تفعيل التسجيل الهيكلي (Structured logging) باستخدام Serilog في الباك اند.
- إعداد مسارات الـ CI/CD.
- إضافة أدوار جديدة مثل (طبيب / موظف استقبال).
